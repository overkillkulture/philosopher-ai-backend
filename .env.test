# Test Environment Variables
NODE_ENV=test
JWT_SECRET=test-secret-key-for-jest
PORT=3002
CORS_ORIGIN=*

# Test Database (in-memory SQLite)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=test_db
DB_USER=test
DB_PASSWORD=test

# Mock API Keys (not real)
ANTHROPIC_API_KEY=test_mock_key
STRIPE_SECRET_KEY=test_mock_key
STRIPE_WEBHOOK_SECRET=test_mock_key

# Rate Limiting (relaxed for tests)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000
